server:
  port: 8761

spring:
  application.name: eureka-discovery-service
  cloud:
    config:
      import-check:
        enabled: false

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
